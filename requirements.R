# Required packages for superbug_monte_carlo.Rmd
# Run this script once to install all dependencies

install.packages("knitr")
install.packages("rmarkdown")

# All simulation code uses base R only — no additional packages required
